project_name: wiz
layers:
  - layer: controller
    tag: http
  - layer: service
  - layer: repository
    tag: postgres
models:
  - name: User
    fields:
      - name: Username
        type: string
      - name: Age
        type: uint8
      - name: LongDescriptionField
        type: string
    methods:
      - "Create"
      - "Update"
      - "Delete"
      - "CastSpell"
