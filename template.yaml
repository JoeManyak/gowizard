project_name: wiz
layers:
  controller: http
  service:
  repository: postgres
models:
  - name: User
    fields:
      - name: Username
        type: string
      - name: Age
        type: uint8
      - name: LongDescriptionField
        type: string
    methods:
      - "Create"
      - "Update"
      - "Delete"
      - "CastSpell"
